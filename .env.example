# Collect every 1 minute (Docker app default)
SCRAPE_INTERVAL_SECONDS=60
# Log level: debug | info | warn | error (JSON logs to stdout/stderr)
LOG_LEVEL=info
PORT=3000
TZ=Australia/Perth
# Optional: lock CORS to your ngrok origin, e.g. https://abc123.ngrok-free.app
# CORS_ORIGIN=*
